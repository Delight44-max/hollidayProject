import CartClient from '@/components/CartClient';

export const metadata = {
  title: 'Shopping Cart - Semicolon Store',
  description: 'Review and checkout your items.',
};

export default function CartPage() {
  return (

    <CartClient />
  );
}